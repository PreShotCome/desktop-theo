appId: com.preshotcome.theodesktop
productName: Theo Desktop
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml,package-lock.json}'
win:
  target:
    - nsis
  artifactName: ${productName}-${version}-setup.${ext}
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  shortcutName: ${productName}
  createDesktopShortcut: true
# Auto-update feed. The installed app reads app-update.yml (generated from this)
# and polls this URL for latest.yml + the new installer. Static public host, so
# no token is needed. Point this at your dedicated Firebase Hosting site.
publish:
  - provider: generic
    url: https://theo-desktop-updates.web.app
